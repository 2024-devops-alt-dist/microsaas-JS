# frontend communicates with backend using these values
# when running locally the API listens on 3000 inside the container
NEXT_PUBLIC_API_BASE_URL=
NEXT_PUBLIC_API_HOST=localhost
NEXT_PUBLIC_API_PORT=3000
